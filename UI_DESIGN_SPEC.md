# 小简攒钱打卡 - UI 与分享海报设计规范 (v2.0 情感化/女性向升级版)

## 一、 视觉设计基调与目标用户分析
通过对小红书平台上高赞小说类、打卡类产品（如：言情小说分享图、喵喵记账、网易蜗牛读书海报等）的深度调研，我们得出针对**女性目标用户（18-35岁）**的审美偏好核心要素：
1. **拒绝干瘪与简陋**：纯色背景和简单的卡片已经无法满足“分享欲”。需要有**氛围感**、**温度感**。
2. **色彩与材质**：偏爱“水彩晕染”、“极光渐变”、“磨砂玻璃（Glassmorphism）”等具有光影变化的高级质感。
3. **情感共鸣**：分享海报不仅是晒数据，更是晒“生活态度”。**情感金句（Quotes）**和**优雅的排版（Serif 衬线体结合）**是激发分享的核心。
4. **错落有致的排版**：打破常规的对称和对齐，采用杂志风（Magazine Layout）排版，增加留白、印章、手写体等装饰元素。

---

## 二、 升级版色彩与材质系统 (Color & Texture)

### 1. 氛围感渐变背景 (Ambient Gradients)
不再使用单一的高级灰，而是采用具有呼吸感的微渐变：
- **主背景极光晕染**：`#FFF5F5` (晨曦粉) 过渡到 `#F0F4FF` (极光蓝) 过渡到 `#FDFBF7` (奶油白)。
- **海报专属水彩背景**：`linear-gradient(135deg, #FFD1D1 0%, #FFECD2 50%, #E2F0FF 100%)`

### 2. 玻璃拟态卡片 (Glassmorphism Cards)
用于首页核心卡片和海报主体：
- **背景色**: `rgba(255, 255, 255, 0.7)`
- **高斯模糊 (Blur)**: `backdrop-filter: blur(20px)`
- **高光边框**: `border: 1px solid rgba(255, 255, 255, 0.8)`
- **弥散阴影**: `box-shadow: 0 16px 40px rgba(255, 130, 122, 0.15)`

### 3. 文字色彩 (Typography Colors)
- **核心数据**: `#FF7E79` (玫瑰桃粉，饱和度适中，温柔且醒目)
- **金句/诗意文本**: `#5A4A42` (暖咖色，替代冷硬的纯黑，增加文艺感)
- **辅助文本**: `#A39692` (浅茶色)

---

## 三、 分享海报专属排版规范 (Poster Layout Spec)

### 1. 海报尺寸与结构 (9:16 比例)
- **画布尺寸**: `750px * 1334px` (标准朋友圈/小红书分享比例)
- **结构划分**:
  - **Top (20%)**: 顶部情绪留白区，放置日期、天气、或者是手写体的小简 Logo。
  - **Middle (50%)**: 核心展示区。采用“磨砂玻璃卡片”，内部大字号展示「总金额」与「连续打卡天数」，并配有**精美的引言/金句**（如：“时间看得见你的每一分努力”）。
  - **Bottom (30%)**: 底部互动区。左侧为品牌 Slogan，右侧为优雅的圆形「小程序码」和“长按扫码，开启自律”提示。

### 2. 字体选择 (Font Stack)
- **数字**: `DIN Alternate` 或 `Georgia`（数字带有一点古典优雅感）。
- **金句/引言**: 建议在 Canvas 中采用宋体/明体（`Songti SC`, `STSong`），增加杂志感。
- **常规排版**: `PingFang SC` (细体/常规体)。

### 3. 装饰元素 (Decorative Elements)
- **引号**: 超大号、低透明度的引号 `“ ”` 作为背景装饰。
- **进度光环**: 不是干瘪的线条，而是带有两端圆角、渐变发光的优美弧线。
- **抽象几何**: 画面四角放置一些模糊的圆形色块，模拟光斑（Bokeh）。

---

## 四、 页面 UI 同步优化指导
1. **首页背景**: 加入两个绝对定位的、模糊的彩色圆形（如一个粉色、一个浅蓝），在底层作为氛围光晕。
2. **卡片质感**: 将白底卡片升级为半透明亚克力质感，增加 `border: 1px solid #FFF`。
3. **交互文案**: 
   - “攒一笔” -> “记录今日小确幸” / “存入星光” (可选，根据实际测试反馈调整，目前保留“攒一笔”但增加动态提示)。
   - 目标列表的文字排版更加松弛，数字放大。
4. **生成动作**: 点击分享后，弹出一个极具仪式感的“生成中...”动画，随后展示高保真海报。